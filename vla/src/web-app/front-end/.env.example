# Lang2Pick Web Application - Example Environment Configuration
# Copy this file to .env.local and customize for your setup

# ============================================================================
# API Configuration
# ============================================================================

# REST API server URL (Python FastAPI backend)
# Default: http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL for real-time updates (joint states, task progress)
# Default: ws://localhost:8000/ws
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# WebRTC signaling server URL (for live camera feed)
# Default: http://localhost:8080
NEXT_PUBLIC_WEBRTC_URL=http://localhost:8080

# gRPC proxy URL (optional, if using REST-to-gRPC proxy)
NEXT_PUBLIC_GRPC_PROXY_URL=http://localhost:8000/grpc

# ============================================================================
# Feature Flags
# ============================================================================

# Enable WebRTC video streaming from robot camera
# Set to 'true' to connect to WebRTC server for live feed
# Set to 'false' to use local camera as fallback
NEXT_PUBLIC_WEBRTC_ENABLED=false

# Enable simulation mode (uses dummy data, no real robot connection)
# Set to 'true' for development/demo without robot hardware
# Set to 'false' to connect to real robot via ROS2
NEXT_PUBLIC_USE_SIMULATION=true

# ============================================================================
# Backend Service URLs (Server-side only - not exposed to browser)
# ============================================================================

# gRPC detection service URL
GRPC_BACKEND_URL=http://localhost:50051

# ROS2 Bridge (rosbridge) WebSocket URL
# Used for communicating with ROS2 nodes
ROS2_BRIDGE_URL=http://localhost:9090

# ============================================================================
# Production Configuration Example
# ============================================================================

# For production deployment, update these values:
#
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
# NEXT_PUBLIC_WS_URL=wss://api.your-domain.com/ws
# NEXT_PUBLIC_WEBRTC_URL=https://webrtc.your-domain.com
# NEXT_PUBLIC_WEBRTC_ENABLED=true
# NEXT_PUBLIC_USE_SIMULATION=false
# GRPC_BACKEND_URL=http://detection-service:50051
# ROS2_BRIDGE_URL=http://rosbridge:9090
